on:
  workflow_dispatch:
    inputs:
      example_parameter:
        description: 'An example input'
        required: false
        default: 'value'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: echo "Workflow triggered from Azure Function App"
